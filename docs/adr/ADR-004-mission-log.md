# ADR-004 — Mission Log semantic mirror (mandatory)
**Status:** Accepted
Every CelestialBody carries a required `log` entry rendered as SSR'd semantic HTML at /log and as an in-experience panel. Solves a11y + SEO + no-WebGL fallback. Rationale: blueprint §0 D4, §15.
